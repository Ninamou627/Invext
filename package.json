{
  "name": "invext",
  "version": "1.0.0",
  "description": "Platform d'investissement et trading en temps réel avec anályse de marché",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend && npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm start --workspace=backend && npm start --workspace=frontend",
    "test": "npm test --workspace=backend && npm test --workspace=frontend"
  }
}
